CHIEF Frontend

This is a minimal Vite + React frontend for the CHIEF dashboard (Sprint 3).

To run locally:

1. cd frontend
2. npm install
3. npm run dev

The app is intentionally standalone and does not integrate with the backend yet.